<img
  src="{{ attr.src }}"
  {% if attr.alt is defined %}alt="{{ attr.alt }}"{% endif %}
  {% if attr.width is defined %}width="{{ attr.width }}"{% endif %}
  {% if attr.height is defined %}height="{{ attr.height }}"{% endif %}
  >
